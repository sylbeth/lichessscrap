SELECT FCId FROM FinalConfiguration
WHERE EndBlackPieces = :black_pieces AND EndWhitePieces = :white_pieces AND ARow = :a_row AND BRow = :b_row AND CRow = :c_row AND DRow = :d_row AND ERow = :e_row AND FRow = :f_row AND GRow = :g_row AND HRow = :h_row;
